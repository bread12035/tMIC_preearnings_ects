apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tmic

resources:
  - service-account.yaml
  - configmap-shared.yaml
  - configmap-pre-earnings.yaml
  - configmap-ects.yaml
  - configmap-calendar-sync.yaml
  - configmap-task-dispatcher.yaml
  - configmap-prompts.yaml
  # secret.yaml is intentionally NOT listed; manage with sealed-secrets / SOPS
  # / External Secrets Operator. secret.yaml.example shows the shape only.
  - pre-earnings-deployment.yaml
  - ects-deployment.yaml
  - calendar-sync-cronjob.yaml
  - task-dispatcher-cronjob.yaml
